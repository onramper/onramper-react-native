///
/// HybridOnramperCheckoutButtonComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "HybridOnramperCheckoutButtonComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <NitroModules/PropNameIDCache.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::onramper::views {

  extern const char HybridOnramperCheckoutButtonComponentName[] = "OnramperCheckoutButton";

  HybridOnramperCheckoutButtonProps::HybridOnramperCheckoutButtonProps(const react::PropsParserContext& context,
                                                                       const HybridOnramperCheckoutButtonProps& sourceProps,
                                                                       const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    intentHandle([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("intentHandle", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.intentHandle;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.intentHandle);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("OnramperCheckoutButton.intentHandle: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridOnramperCheckoutButtonSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridOnramperCheckoutButtonSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("OnramperCheckoutButton.hybridRef: ") + exc.what());
      }
    }()) { }

  bool HybridOnramperCheckoutButtonProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("intentHandle"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridOnramperCheckoutButtonComponentDescriptor::HybridOnramperCheckoutButtonComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridOnramperCheckoutButtonComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                                  const std::shared_ptr<const react::Props>& props,
                                                                                                  react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridOnramperCheckoutButtonShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridOnramperCheckoutButtonComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = static_cast<HybridOnramperCheckoutButtonShadowNode&>(shadowNode);
    const std::shared_ptr<const HybridOnramperCheckoutButtonProps>& constProps = concreteShadowNode.getConcreteSharedProps();
    const std::shared_ptr<HybridOnramperCheckoutButtonProps>& props = std::const_pointer_cast<HybridOnramperCheckoutButtonProps>(constProps);
    HybridOnramperCheckoutButtonState state{props};
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::onramper::views
