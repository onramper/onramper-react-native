///
/// HybridOnramperNitroSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif



#include <string>
#include <NitroModules/Promise.hpp>
#include <functional>

namespace margelo::nitro::onramper {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `OnramperNitro`
   * Inherit this class to create instances of `HybridOnramperNitroSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridOnramperNitro: public HybridOnramperNitroSpec {
   * public:
   *   HybridOnramperNitro(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridOnramperNitroSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridOnramperNitroSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridOnramperNitroSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<std::string>> ping(const std::string& message) = 0;
      virtual std::shared_ptr<Promise<std::string>> sdkProbe() = 0;
      virtual std::shared_ptr<Promise<void>> startTicker(const std::function<void(double /* count */)>& onTick) = 0;
      virtual std::shared_ptr<Promise<void>> stopTicker() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "OnramperNitro";
  };

} // namespace margelo::nitro::onramper
